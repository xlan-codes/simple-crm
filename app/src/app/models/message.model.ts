

export class MessageModel {
    message: string; // hold the message
    room: string; // room that we want to send the message
    userId: string; // userid that send the message
    time: Date; // time that we send the message
}
